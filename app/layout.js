import './globals.css'
export const metadata = { title: 'DibbyTour - Marketplace Safety', description: 'Check any listing before you buy' }
export default function RootLayout({ children }) {
  return <html lang="en"><body className="bg-black text-white">{children}</body></html>
}
